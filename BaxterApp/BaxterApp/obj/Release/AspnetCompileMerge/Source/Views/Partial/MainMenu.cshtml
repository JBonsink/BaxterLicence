@{
    Layout = null;
}

<nav class="navbar navbar-default navbar-fixed-bottom" role="navigation">
    <div class="container">
        <div class="navbar-header">
            <button type="button" class="navbar-toggle collapsed" data-toggle="collapse" data-target="#collapse-bottom-menu">
                <span class="sr-only">Toggle navigation</span>
                <span class="icon-bar"></span>
                <span class="icon-bar"></span>
                <span class="icon-bar"></span>
            </button>
        </div>
        <div class="collapse navbar-collapse" id="collapse-bottom-menu">
            <ul class="nav navbar-nav">
                <!--<li>@Html.ActionLink(Resources.Global.General_Home, "Index", "Home")</li>-->
                @if (ViewBag.ViewUser)
                {
                    <li class="dropdown">
                        <a href="#" class="dropdown-toggle" data-toggle="dropdown">@Resources.Global.General_Users<span class="caret"></span></a>
                        <ul class="dropdown-menu" role="menu">
                            @if (ViewBag.ViewLog)
                            {
                                <li>@Html.ActionLink(Resources.Global.General_Logs, "Index", "Log")</li>
                                <li class="divider"></li>
                            }
                            @if (ViewBag.ViewRole)
                            {
                                <li>@Html.ActionLink(Resources.Global.General_Roles, "Index", "Role")</li>
                            }
                            @if (ViewBag.ViewUser)
                            {
                                <li>@Html.ActionLink(Resources.Global.General_Users, "Index", "User")</li>
                            }
                        </ul>
                    </li>
                }
                @if (ViewBag.ViewMaintenance)
                {
                <li class="dropdown">
                    <a href="#" class="dropdown-toggle" data-toggle="dropdown">@Resources.Global.General_Maintenance<span class="caret"></span></a>
                    <ul class="dropdown-menu" role="menu">
                        <li>@Html.ActionLink(Resources.Global.General_Backups, "Index", "Backup")</li>
                        <li>@Html.ActionLink(Resources.Global.General_Drive, "Index", "Drive")</li>    
                        <li>@Html.ActionLink(Resources.Global.General_AppLogs, "Index", "AppLog")</li>                                                                     
                    </ul>
                </li>
                }
            </ul>
        </div>
    </div>
</nav>
        
           
           
        

